# Lambda Requirements File
# 
# Install these dependencies when deploying your Lambda functions
# 
# For performance_waste_analysis_lambda.py:
boto3>=1.28.0
botocore>=1.31.0

# For performance_ai_analysis_lambda.py:
boto3>=1.28.0
botocore>=1.31.0

# Optional: For enhanced JSON handling
python-dateutil>=2.8.0

# Note: These are the minimum versions. AWS Lambda runtime typically includes
# boto3 and botocore, but specifying versions ensures compatibility.
#
# To create a deployment package:
# 1. Create a directory for your function
# 2. Copy your .py file into it
# 3. Run: pip install -r requirements.txt -t .
# 4. Zip the entire directory
# 5. Upload to AWS Lambda